# 网络风险监控 接口文档

> 版本：v1.0  
> 更新日期：2026-08-11  
> 说明：前端目前使用 `src/mock/riskApi.js` 中的 Mock 实现联调，后端接口就绪后将 `fetchRiskDetails` 替换为真实请求即可。

---

## 一、接口总览

| 序号 | 接口名称 | 请求方式 | 路径 | 用途 |
| :--: | :--- | :--: | :--- | :--- |
| 1 | 查询网络风险详情列表 | POST | `/api/risk/check-ne-details` | 网络风险详情弹窗的分页列表、列筛选项、总条数 |
| 2 | 查询风险概览统计 | POST | `/api/risk/overview` | 概览卡片的总数、未关闭/已关闭、按风险类型/网元类型统计 |

---

## 二、枚举字典

下列码值在后端落库时可自行定义，前端按码值传参、按展示值渲染。若后端直接返回展示值，则前端无需二次映射。

### 2.1 风险状态 `risk_status`

| 码值 | 展示值 |
| :--: | :--: |
| `ALL` | 全部 |
| `UNCLOSED` | 未关闭 |
| `CLOSED` | 已关闭 |

### 2.2 风险类型 `risk_type_code`

| 码值 | 展示值 |
| :--: | :--: |
| `CONFIG` | 配置类 |
| `PERFORMANCE` | 性能类 |
| `SECURITY` | 安全类 |

### 2.3 风险等级 `risk_level_code`

| 码值 | 展示值 |
| :--: | :--: |
| `HIGH` | 高 |
| `MEDIUM` | 中 |
| `LOW` | 低 |

### 2.4 网元类型 `ne_type_code`

| 码值 | 含义 |
| :--: | :--- |
| `AMF` | 接入与移动性管理功能 |
| `SMF` | 会话管理功能 |
| `UPF` | 用户面功能 |
| `MME` | 移动性管理实体 |
| `AUSF` | 鉴权服务功能 |
| `UDM` | 统一数据管理 |
| `PCF` | 策略控制功能 |
| `UDR` | 统一数据仓库 |
| `NSSF` | 网络切片选择功能 |
| `NRF` | 网络仓库功能 |

---

## 三、接口详情

### 1. 查询网络风险详情列表

网络风险详情弹窗的数据源。支持按风险状态、风险类型、网元类型进行范围筛选（来自卡片入口点击），并在表格列上做二次筛选（网元ID、网元类型、风险名称、风险类型、风险等级）与分页。

- **请求方式**：`POST`
- **请求路径**：`/api/risk/check-ne-details`
- **Content-Type**：`application/json`
- **Mock 函数**：`fetchRiskDetails(params)` （对应后端存储过程 `cs_ncd_risk_check_ne_details`）

#### 1.1 请求参数

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
| :--- | :---: | :--: | :--: | :--- |
| `start` | int | 否 | `0` | 分页起始偏移量（从 0 开始） |
| `limit` | int | 否 | `15` | 每页条数，可选 10 / 15 / 20 |
| `risk_status` | string | 否 | `ALL` | 风险状态码，见 [2.1](#21-风险状态-risk_status) |
| `risk_type_code` | string | 否 | 空 | 风险类型码，见 [2.2](#22-风险类型-risk_type_code) |
| `ne_type_code` | string | 否 | 空 | 网元类型码，见 [2.4](#24-网元类型-ne_type_code) |
| `risk_level_code` | string | 否 | 空 | 风险等级码，见 [2.3](#23-风险等级-risk_level_code) |
| `ne_id` | string | 否 | 空 | 网元 ID（精确匹配，如 `ALT-0721-156`） |
| `risk_name` | string | 否 | 空 | 风险名称（精确匹配，如 `网元脱管风险`） |

> **筛选优先级说明**
> - `risk_status`、`risk_type_code`、`ne_type_code` 属于「范围筛选」，由卡片入口决定，决定本次查询的数据范围；
> - `ne_id`、`risk_name`、`risk_level_code` 属于「列筛选」，由弹窗表头下拉菜单触发，在范围内再做精确过滤；
> - `filter_options`（响应字段）只受**范围筛选**影响，不受列筛选影响，以保证下拉选项在列筛选后不会变空。

#### 1.2 卡片入口与入参映射

| 卡片点击位置 | `risk_status` | `risk_type_code` | `ne_type_code` |
| :--- | :--: | :--: | :--: |
| 查看全量 / 风险总数 | `ALL` | 空 | 空 |
| 未关闭 | `UNCLOSED` | 空 | 空 |
| 已关闭 | `CLOSED` | 空 | 空 |
| 按风险类型（配置类/性能类/安全类） | `UNCLOSED` | 对应类型码 | 空 |
| 按网元类型（AMF/SMF/…） | `UNCLOSED` | 空 | 对应网元类型码 |

#### 1.3 请求示例

```json
POST /api/risk/check-ne-details
Content-Type: application/json

{
  "start": 0,
  "limit": 10,
  "risk_status": "UNCLOSED",
  "risk_type_code": "CONFIG",
  "ne_type_code": "",
  "risk_level_code": "",
  "ne_id": "",
  "risk_name": ""
}
```

#### 1.4 响应参数

| 字段名 | 类型 | 说明 |
| :--- | :---: | :--- |
| `total_count` | int | 符合全部筛选条件的记录总条数（用于分页计算） |
| `results` | array&lt;object&gt; | 当前页风险记录列表，按 `start`/`limit` 截取 |
| `filter_options` | object | 表头列筛选项的可选值集合（仅受范围筛选影响） |

**`results` 元素结构：**

| 字段名 | 类型 | 说明 | 示例 |
| :--- | :---: | :--- | :--- |
| `id` | string | 风险记录唯一编号 | `RISK-2026-0001` |
| `neName` | string | 网元名称 | `SH-AMF-03` |
| `neId` | string | 网元 ID | `ALT-0721-156` |
| `neType` | string | 网元类型（展示值） | `AMF` |
| `riskName` | string | 风险名称 | `网元脱管风险` |
| `riskType` | string | 风险类型（展示值） | `配置类` |
| `riskLevel` | string | 风险等级（展示值） | `高` |
| `solution` | string | 解决措施 | `检查NCE连接配置并恢复管理通道` |
| `status` | string | 风险状态（展示值） | `未关闭` |
| `detectedAt` | string | 发现时间 | `2026-08-01 10:30` |

**`filter_options` 结构：**

| 字段名 | 类型 | 说明 |
| :--- | :---: | :--- |
| `neId` | string[] | 当前范围内出现的网元 ID 列表 |
| `neType` | string[] | 当前范围内出现的网元类型列表 |
| `riskName` | string[] | 当前范围内出现的风险名称列表 |
| `riskType` | string[] | 当前范围内出现的风险类型列表 |
| `riskLevel` | string[] | 当前范围内出现的风险等级列表（按 高/中/低 顺序） |

#### 1.5 响应示例

```json
{
  "total_count": 11,
  "results": [
    {
      "id": "RISK-2026-0001",
      "neName": "SH-AMF-03",
      "neId": "ALT-0721-156",
      "neType": "AMF",
      "riskName": "网元脱管风险",
      "riskType": "配置类",
      "riskLevel": "高",
      "solution": "检查NCE连接配置并恢复管理通道",
      "status": "未关闭",
      "detectedAt": "2026-08-01 10:30"
    },
    {
      "id": "RISK-2026-0003",
      "neName": "SH-AMF-03",
      "neId": "ALT-0721-156",
      "neType": "AMF",
      "riskName": "配置不一致风险",
      "riskType": "配置类",
      "riskLevel": "中",
      "solution": "对比基线配置并同步标准模板",
      "status": "未关闭",
      "detectedAt": "2026-06-03 10:30"
    }
  ],
  "filter_options": {
    "neId": ["ALT-0317-501", "ALT-0721-156", "ALT-0721-188", "ALT-0912-317", "ALT-0912-352"],
    "neType": ["AMF", "UPF"],
    "riskName": ["备份任务连续失败", "网元脱管风险", "配置不一致风险"],
    "riskType": ["配置类"],
    "riskLevel": ["高", "中", "低"]
  }
}
```

#### 1.6 错误响应

| HTTP 状态码 | 业务含义 | 处理建议 |
| :--: | :--- | :--- |
| 200 | 成功 | — |
| 400 | 参数错误 | 检查 `start`/`limit` 是否为非负整数、码值是否合法 |
| 500 | 服务异常 | 前端展示「风险详情加载失败，请重试。」并提供重试入口 |

错误体示例：

```json
{
  "code": "RISK_500",
  "message": "风险详情查询失败"
}
```

---

### 2. 查询风险概览统计

概览卡片（网络风险卡片）的数据源。返回风险总数、未关闭/已关闭数量，以及按风险类型、网元类型分组的未关闭统计。

- **请求方式**：`POST`
- **请求路径**：`/api/risk/overview`

#### 2.1 请求参数

无入参（统计范围为近 3 个月全量数据，时间范围由后端控制）。

> 如需灵活控制统计周期，可扩展 `period_months`（int，默认 3）参数。

#### 2.2 响应参数

| 字段名 | 类型 | 说明 | 示例 |
| :--- | :---: | :--- | :--- |
| `title` | string | 卡片标题 | `网络风险` |
| `periodLabel` | string | 统计周期展示文案 | `3个月` |
| `total` | int | 风险总数 | `40` |
| `open` | int | 未关闭数量 | `24` |
| `closed` | int | 已关闭数量 | `16` |
| `riskTypes` | array&lt;object&gt; | 按风险类型统计（未关闭） | 见下 |
| `neTypes` | array&lt;object&gt; | 按网元类型统计（未关闭，默认 10 项） | 见下 |

**`riskTypes` / `neTypes` 元素结构：**

| 字段名 | 类型 | 说明 | 示例 |
| :--- | :---: | :--- | :--- |
| `name` | string | 类型名称 | `配置类` / `AMF` |
| `count` | int | 未关闭数量 | `11` |

#### 2.3 响应示例

```json
{
  "title": "网络风险",
  "periodLabel": "3个月",
  "total": 40,
  "open": 24,
  "closed": 16,
  "riskTypes": [
    { "name": "配置类", "count": 11 },
    { "name": "性能类", "count": 7 },
    { "name": "安全类", "count": 6 }
  ],
  "neTypes": [
    { "name": "AMF", "count": 4 },
    { "name": "SMF", "count": 4 },
    { "name": "UPF", "count": 4 },
    { "name": "MME", "count": 4 },
    { "name": "AUSF", "count": 2 },
    { "name": "UDM", "count": 2 },
    { "name": "PCF", "count": 1 },
    { "name": "UDR", "count": 1 },
    { "name": "NSSF", "count": 1 },
    { "name": "NRF", "count": 1 }
  ]
}
```

> **默认值约定**
> - 风险类型统计默认返回 3 项（配置类、性能类、安全类）；
> - 网元类型统计默认返回 10 项（AMF、SMF、UPF、MME、AUSF、UDM、PCF、UDR、NSSF、NRF）。

---

## 四、前后端联调说明

1. **Mock 切换**：`src/mock/riskApi.js` 中 `fetchRiskDetails` 为 Mock 实现，后端就绪后替换为真实 `POST` 请求即可，入参/出参保持本文档约定不变。
2. **码值一致性**：请求体使用码值（如 `CONFIG`、`UNCLOSED`、`HIGH`），响应体使用展示值（如 `配置类`、`未关闭`、`高`）。后端若直接返回码值，前端需在 `riskApi.js` 补充反向映射。
3. **列筛选依赖**：`filter_options` 必须只受范围筛选（`risk_status`/`risk_type_code`/`ne_type_code`）影响，不能受列筛选（`ne_id`/`risk_name`/`risk_level_code`）影响，否则列筛选后下拉选项会变空。
4. **分页**：前端使用 `start`（偏移量）+ `limit`（条数）模式，`total_count` 用于计算总页数。
5. **统计口径**：概览卡片的 `riskTypes`/`neTypes` 统计均为「未关闭」数量，与详情弹窗点击风险类型/网元类型时 `risk_status=UNCLOSED` 的口径一致。
